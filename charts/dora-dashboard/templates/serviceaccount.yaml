{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dora.serviceAccountName" . }}
  labels:
    {{- include "dora.labels" . | nindent 4 }}
{{- end }}
